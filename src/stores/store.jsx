import { atom } from "jotai";

// ! user profile store
export const userProfile = atom(null) 

// ! check if user visit today
export const checkIp = atom(false)

// ! check if user visit blog
export const checkViews = atom(false)